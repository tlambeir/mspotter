{% extends 'base.html.twig' %}

{% block body %}
    <h1>Shop</h1>

    <ul id="navigation">
        {% for product in products %}
            <li><a href="{{ product.id }}">{{ product.name }}</a> - {{ product.category.name }}</li>
        {% endfor %}
    </ul>
{% endblock %}